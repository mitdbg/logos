Screen {
    background: $surface;
}

.screen-title {
    text-style: bold;
    color: $accent;
    padding: 1 2;
}

.section-title {
    text-style: bold;
    color: $text;
    padding: 0 1;
    margin-top: 1;
}

.pane {
    border: solid $primary-darken-2;
    padding: 0 1;
    margin: 0 1;
}

.pane-title {
    text-style: bold;
    color: $accent;
    padding: 0 0 1 0;
}

.error-label {
    color: $error;
}

.success-label {
    color: $success;
}

.muted {
    color: $text-muted;
}

Button {
    margin: 0 1 1 0;
}

Input {
    margin-bottom: 1;
}

Select {
    margin-bottom: 1;
}

DataTable {
    height: 1fr;
}

LoadingIndicator {
    height: 3;
}

/* Exit bar — docked to the bottom of every screen */
.exit-bar {
    dock: bottom;
    height: 3;
    background: $surface-darken-1;
    border-top: solid $primary-darken-2;
    align: right middle;
    padding: 0 1;
}

.exit-bar Button {
    margin: 0 0 0 1;
    min-width: 22;
}
